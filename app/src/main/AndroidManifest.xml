<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.finalproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ResetPassword"></activity>
        <activity android:name=".BlankActivity" />
        <activity android:name=".ADMIN.AdminGradesActivity" />
        <activity android:name=".STUDENT.GradesActivity" />
        <activity android:name=".STUDENT.QuizAnswersActivity" />
        <activity android:name=".ADMIN.AssignQuizActivity" />
        <activity android:name=".ADMIN.AssignStudentQuizActivity" />
        <activity android:name=".ADMIN.AdminActivity" />
        <activity android:name=".ADMIN.CreateQuestionActivity" />
        <activity android:name=".ADMIN.QuizLayoutActivity" />
        <activity android:name=".STUDENT.Quiz" />
        <activity android:name=".Student" />
        <activity android:name=".STUDENT.Result" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>